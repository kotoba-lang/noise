{
  "name": "kotoba-noise",
  "private": true,
  "description": "npm deps for the ClojureScript primitive provider (noise.provider.noble). JVM consumers need none of this.",
  "dependencies": {
    "@noble/ciphers": "^1.2.1",
    "@noble/curves": "^1.8.1",
    "@noble/hashes": "^1.7.1"
  },
  "devDependencies": {
    "nbb": "^1.4.208"
  },
  "scripts": {
    "test": "nbb --classpath src:test run-tests.cljs"
  }
}
